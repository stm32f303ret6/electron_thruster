Paste everything below this line into a Claude Code session on the GPU machine.
================================================================================

Launch and gate the pre-registered PIC characterization spoke
`characterization.350V_400km` (350 V) in this repo.

CONTEXT
- Repo: the electron_thruster repo (this file lives at
  pic_sims/characterization/350V_400km/GPU_SESSION_PROMPT.txt; the repo
  root is four directories up). Branch: chipsat2. If the working tree is
  missing the 350V_400km/ folder, pull the latest commit first.
- The spoke is fully scaffolded and pre-registered (2026-08-17): config.yaml
  (350 V / 0.793 mA), acceptance.yaml (policy
  characterization.350V_400km.v1), plan in README.md, engine files
  copied verbatim from the PASSed high_thrust spoke. Tests pass (36/37; the
  one failure on old numpy is environmental and should not occur in the warpx
  env). Do NOT edit config.yaml or acceptance.yaml — they are the
  pre-registration. If something forces a physics/gate change, stop and
  report instead of editing.
- Purpose: the model's 400 km axial row demands V_min = 304 V, just above the
  tested 300 V ceiling. This run extends the measured envelope to 350 V.
  Pre-registered predictions: F ~ 40.5 nN (frontier F ~ V^2), phi ~ 47 V
  (fitted collection law) — deliberately close to the required 50 V benign
  gate. A FAIL on that gate is a valid negative result (charging tax), not a
  reason to rerun or tune.

STEPS
1. Environment: conda activate warpx-cpu-mpich-dev (the conda command may be
   mamba or micromamba). Confirm a GPU is free (nvidia-smi) — the campaign
   convention is one run per GPU, never concurrent with another spoke run.
2. Sanity before launch, from pic_sims/characterization/350V_400km/:
     PYTHONNOUSERSITE=1 python -m pytest tests/ -q        # expect all pass
     python - <<'EOF'
     from helpers import load_config
     c = load_config("config.yaml")
     assert c.stage_id == "characterization.350V_400km"
     assert c.V_GAP == 350.0 and abs(c.i_beam - 0.793e-3) < 1e-9
     print("dt[ps]", round(c.dt*1e12,3), "steps", int(800e-9/c.dt))  # ~3.859, ~207k
     EOF
3. Launch (expect ~8–9 h; the 300 V run took ~8 h at ~193k steps, this one is
   ~207k):
     python simulation.py
   Run it in the background / keep the session alive; check progress
   periodically via the run's log under outputs/<run-id>/ (the deck prints
   step/phi_body progress; watch that phi_body approaches a plateau and the
   choke watchdog (100 V) stays quiet).
4. If the run aborts with the choke detector (sustained phi > 100 V): that is
   a FAILED run by design — commit nothing, report the log tail and stop.
5. Analyze and gate the COMPLETED run:
     python analyze.py --run outputs/<run-id> --policy acceptance.yaml
   Report the verdict and the measured metrics (escape %, f_beam_nN,
   phi_body_V, current balance, edge phi, ledger consistencies) next to the
   pre-registered predictions (40.5 nN, 47 V, >=95 %).
6. Whatever the verdict, do not tune and rerun. One committed attempt. If the
   verdict is PASS, snapshot the evidence the same way the other spokes do
   (reference_results/<run-id>/ with metrics.json, verdict.json,
   config_used.yaml, acceptance_used.yaml, run manifest, figures) — follow
   the structure of ../high_thrust/reference_results/ exactly.
7. Update docs ONLY after a committed verdict, keeping the honest-framing
   rules (state power demand, never solar-harvest closure verdicts; 400 km
   remains "envelope-covered, mission open" even on PASS — drag maxima,
   night rows, attitude, and ~150–200 mW mean demand all stay open):
   - pic_sims/characterization/README.md: replace the 350V_400km row's
     "pre-registered … no run yet" with the gated result.
   - pic_sims/characterization/350V_400km/README.md: add a results
     section (keep the pre-registered plan text intact above it).
   - CAMPAIGN.md and model/MODEL.md section 4's note on 400 km: update only
     if the verdict changes their statements.
   - README2.md (the reviewer-facing draft README at repo root) — update it
     precisely as follows, matching its plain, minimal style:

     IF ALL REQUIRED GATES PASS (float <= 50 V):
     a. "Thrust law" section: change "across the full 100–300 V range" to
        "across the full 100–350 V range" ONLY if analyze confirms the
        measured F still fits the two-constant law to ~1 %; otherwise leave
        the range at 100–300 V and note the 350 V deviation instead.
     b. "Does it cancel drag?" intro sentence: after the 30.13 nN clause,
        append the measured 350 V point, e.g. "and <F> nN at 350 V"
        (use the measured value, not the 40.5 nN prediction).
     c. The minimum-voltage demand table: change every "(≤ 300 V)" to
        "(≤ 350 V)", and change the 400 km axial row's last cell from
        "no — just above the 300 V ceiling" to "yes — inside the extended
        350 V envelope" (its V_min is 304 V).
     d. Replace the "Note on 400 km" paragraph with one that says: the
        axial-pose demand is now inside the measured envelope (350 V run,
        <F> nN measured vs 32.9 nN mean demand, float <phi> V); what stays
        open at 400 km — drag maxima (92.4 nN) above any single operating
        point, night-side plasma rows still extrapolated, ~150–200 mW mean
        power as mission design, and the lateral-pose thrust-axis redesign.
        Keep it 3–4 sentences, no hype.
     e. Motivation bullet: change "with 400 km in reach of design work" to
        "with the 400 km mean demand covered at 350 V (mission-level
        questions still open)".
     f. "PIC simulations — characterization" table: add a row for
        `350V_400km` | drive voltage 350 V | measured phi and F, PASS
        date — same format as the other rows (it becomes seven spokes;
        update the "Six spokes" sentence to "Seven spokes" and
        "All six" to "All seven").

     IF body_floats_benign FAILS (float > 50 V) but the run is otherwise
     clean:
     - Do NOT change the envelope wording or the 400 km table row; the
       envelope claim stays at 300 V because the 350 V point is outside the
       design limit.
     - Update the "Note on 400 km" to state the new measured fact: pushing
       to the 400 km-enabling drive (350 V) drove the float to <phi> V,
       past the 50 V design limit — 400 km carries a measured charging tax
       at this geometry, and the slender-body direction is the open route.
     - Add the characterization table row with the honest result (e.g.
       "gated FAIL — float <phi> V exceeds the 50 V design limit; measured
       finding, evidence committed").

     IF only the non-required covers_400km_mean_drag gate misses (F < 32.9
     nN with float benign): keep the envelope updates (a–c, f) with the
     measured F, but in (d) state that the envelope extends while the
     measured thrust falls short of the 400 km mean demand by the measured
     margin — a finding about the frontier scaling. Skip (e).

     In every case: never reintroduce closes/does-not-close power verdicts
     or solar-harvest gating; power stays framed as demand.
8. Commit with a message in the campaign's style (see git log for examples:
   short imperative subject stating the measured result), and include the
   Co-Authored-By trailer your session uses. Do not push unless asked.

REPORT BACK
End by summarizing: verdict (PASS/FAIL + which gates), measured vs predicted
table, run id, wall time, and which docs you touched.
