Paste everything below this line into a Claude Code session on the GPU machine.
================================================================================

Launch and gate the pre-registered PIC characterization spoke
`characterization.350V_400km_slender` (slender can, L/r = 6, at 350 V) in
this repo.

CONTEXT
- Repo: the electron_thruster repo (this file lives at
  pic_sims/characterization/350V_400km_slender/GPU_SESSION_PROMPT.txt; the
  repo root is four directories up). Branch: chipsat2. If the working tree
  is missing the 350V_400km_slender/ folder, pull the latest commit first.
- RUN ORDER: this spoke is the fourth corner of the 2x2 voltage x geometry
  factorial and is meant to run AFTER its squat sibling
  `characterization.350V_400km` (see ../350V_400km/GPU_SESSION_PROMPT.txt).
  If the squat 350 V run has not been committed yet, run that one first
  unless the operator explicitly says otherwise.
- The spoke is fully scaffolded and pre-registered (2026-08-17): config.yaml
  (slender geometry + 350 V / 0.793 mA), acceptance.yaml (policy
  characterization.350V_400km_slender.v1), plan in README.md, engine files
  copied verbatim from the PASSed slender_body spoke. Do NOT edit
  config.yaml or acceptance.yaml — they are the pre-registration. If
  something forces a physics/gate change, stop and report instead of
  editing.
- Purpose: test whether the two committed laws COMPOSE — the voltage
  frontier (measured 100-300 V, squat) and the geometry collection law
  (measured at 200 V, slender). Pre-registered predictions:
  phi ~ 11-17 V (composed collection law; far inside the 50 V benign
  gate, unlike the squat can's ~47 V near-miss), F ~ 42-43 nN (frontier
  plus the beam energy the lower float returns), escape >= 95 %. A float
  anywhere near 50 V means the laws do NOT compose — that is a valid
  negative result, not a reason to rerun or tune.

STEPS
1. Environment: conda activate warpx-cpu-mpich-dev (the conda command may be
   mamba or micromamba). Confirm a GPU is free (nvidia-smi) — the campaign
   convention is one run per GPU, never concurrent with another spoke run.
2. Sanity before launch, from pic_sims/characterization/350V_400km_slender/:
     PYTHONNOUSERSITE=1 python -m pytest tests/ -q        # expect all pass
     python - <<'EOF'
     from helpers import load_config
     c = load_config("config.yaml")
     assert c.stage_id == "characterization.350V_400km_slender"
     assert c.V_GAP == 350.0 and abs(c.i_beam - 0.793e-3) < 1e-9
     assert (c.nr, c.nz) == (200, 608)
     assert abs(c.cathode_standoff - 4.7e-3) < 1e-9
     print("dt[ps]", round(c.dt*1e12,3), "steps", int(800e-9/c.dt))  # ~3.859, ~207k
     EOF
3. Launch (expect ~8.5-9 h; the 200 V slender run took ~6.5 h at ~159k
   steps, this one is ~207k on the same 200x608 grid):
     python simulation.py
   Run it in the background / keep the session alive; check progress
   periodically via the run's log under outputs/<run-id>/ (the deck prints
   step/phi_body progress; watch that phi_body approaches a plateau — the
   prediction is ~11-17 V — and the choke watchdog (100 V) stays quiet).
4. If the run aborts with the choke detector (sustained phi > 100 V): that is
   a FAILED run by design — commit nothing, report the log tail and stop.
5. Analyze and gate the COMPLETED run:
     python analyze.py --run outputs/<run-id> --policy acceptance.yaml
   Report the verdict and the measured metrics (escape %, f_beam_nN,
   phi_body_V, current balance, edge phi, ledger consistencies) next to the
   pre-registered predictions (42-43 nN, 11-17 V, >=95 %), and next to the
   squat 350 V result if that run is committed — the factorial comparison
   IS the deliverable.
6. Whatever the verdict, do not tune and rerun. One committed attempt. If the
   verdict is PASS, snapshot the evidence the same way the other spokes do
   (reference_results/<run-id>/ with metrics.json, verdict.json,
   config_used.yaml, acceptance_used.yaml, run manifest, figures) — follow
   the structure of ../slender_body/reference_results/ exactly.
7. Update docs ONLY after a committed verdict, keeping the honest-framing
   rules (state power demand, never solar-harvest closure verdicts; 400 km
   remains "envelope-covered, mission open" even on PASS — drag maxima,
   night rows, attitude, and ~150-200 mW mean demand all stay open):
   - pic_sims/characterization/README.md: replace this spoke's
     "pre-registered ... no run yet" row with the gated result.
   - pic_sims/characterization/350V_400km_slender/README.md: add a results
     section (keep the pre-registered plan text intact above it), including
     the filled-in 2x2 factorial table.
   - SCALING_LAWS.md section 8b: append the 350 V slender point to the
     geometry-scaling evidence ONLY if the measured float confirms (or
     refutes) the composed-law prediction — state which.
   - CAMPAIGN.md, model/MODEL.md section 4, and README2.md: the 400 km
     narrative depends on BOTH 350 V runs. Follow the decision table in
     ../350V_400km/GPU_SESSION_PROMPT.txt step 7 for the envelope wording;
     where that table's FAIL branch says "the slender-body direction is the
     open route", replace it with this run's measured answer. Never
     reintroduce closes/does-not-close power verdicts or solar-harvest
     gating; power stays framed as demand.
