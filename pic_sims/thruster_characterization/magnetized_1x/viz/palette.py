"""Shared color code for the chipsat-thruster figures.

Red = spacecraft body / chassis (floats), blue = cathode (held below the
body by the supply).  Every figure in this case uses the same convention;
if the other capstone cases adopt it, this module is the thing to hoist.
"""

BODY_EDGE = "#a93226"   # red  = spacecraft body / chassis (floats)
BODY_FILL = "#f5c6c0"
CATH_EDGE = "#1a5276"   # blue = cathode (held below the body by the supply)
CATH_FILL = "#cfe3f5"
BEAM = "#2471a3"
AMBIENT = "0.45"
